<nav class="nav-extended primary-navbar">
    <ul class="tabs tabs-transparent">
        <li class="tab">
            <a 
                {% if selected is defined and selected == "show_dashboard_ae_administracion_configuracion_ajustes" %}
                    class="active"
                {% else %}
                    class="show-loader-top"
                    href="{{ path("show_dashboard_ae_administracion_configuracion_ajustes") }}" 
                    target="_self"
                {% endif %}
               >{{ content['section_text']['sas_ajustesConfig'] }}</a>
        </li>
        <li class="tab">
            <a 
                {% if selected is defined and selected == "show_dashboard_ae_administracion_configuracion_conexion" %}
                    class="active"
                {% else %}
                    class="show-loader-top"
                    href="{{ path("show_dashboard_ae_administracion_configuracion_conexion") }}" 
                    target="_self"
                {% endif %}
               >{{ content['section_text']['sas_datosConexionFM'] }}</a>
        </li>
        <li class="tab">
            <a 
                {% if selected is defined and selected == "show_dashboard_ae_administracion_configuracion_estilos" %}
                    class="active"
                {% else %}
                    class="show-loader-top"
                    href="{{ path("show_dashboard_ae_administracion_configuracion_estilos") }}" 
                    target="_self"
                {% endif %}
               >{{ content['section_text']['sas_configEstilos'] }}</a>
        </li>
        <li class="tab">
            <a 
                {% if selected is defined and selected == "visitante_sync" %}
                    class="active"
                {% else %}
                    class="show-loader-top"
                    href="{{ path("visitante_sync") }}" 
                    target="_self"
                {% endif %}
               >Sync FM - PG</a>
        </li>
    </ul>
</nav>